{{!-- * Setup the `index.handlebars` to have the template that Handlebars can render onto.

     * Create a button in `index.handlebars` that will submit the user input into the database. --}}

<h1>Cheesesteak Jawn</h1>

<h3>Cheesesteak Aspirations:</h3>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/unless}}
    {{/each}}

</ul>

<h3>Cheesesteaks I have known and loved: </h3>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/if}}
    {{/each}}
</ul>

<h2>Plan to eat a new Cheesesteak</h2>
<form action="" class="create-form">
    <div class="form-group">
        <label for="burger">Cheesesteak Spot:</label>
        <input type="text" id="burger" name="name">
    </div>

    <button type="submit">Add that Jawn</button>

    {{!-- <div class="form-grou"></div> --}}
</form>